# Database Configuration
POSTGRES_USER=locusum
POSTGRES_PASSWORD=locusum_password
POSTGRES_DB=locusum
POSTGRES_HOST=localhost  # Use 'localhost' for local dev, 'db' is default in Docker
# DATABASE_URL=postgresql://locusum:locusum_password@localhost:5432/locusum

# SQLite Configuration (for buffer)
SQLITE_FILE_NAME=data/locusum_buffer.db

# AI Integration
GOOGLE_API_KEY=your_gemini_api_key_here

# LLM Provider: gemini (default), ollama
LLM_PROVIDER=gemini
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
